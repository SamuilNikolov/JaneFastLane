@model IEnumerable<JaneFastLane.Models.Menu>

@{
    ViewData["Title"] = "Index";
}
<p>
    <a asp-action="Create">Create New</a>
</p>
<section id="menu" class="menu">
    <div class="container" data-aos="fade-up">
        @if (ViewBag.Table != null)
        {

            <div class="section-header">
                <p>
                    Седнали сте на маса <span>№@ViewBag.Table.Number (Тип: @ViewBag.Table.Characteristics.Characteristic)</span>
                </p>
                <p>
                    За Вас е отговорен сервитьор: <span>@ViewBag.Table.Waiter.UserName</span>
                </p>
            </div>


            <section class="h-100" style="background-color: #eee;">
                <div class="container h-100 py-5">
                    <div class="row d-flex justify-content-center align-items-center h-100">
                        <div class="col-10">

                            <div class="text-center mb-4">
                                <h1 class="fw-normal mb-0 text-black" style="font-style:italic">Количка</h1>
                            </div>

                            @foreach (var item in ViewBag.Cart)
                            {
                                <div class="card rounded-3 mb-4">
                                    <div class="card-body p-4">
                                        <div class="row d-flex justify-content-between align-items-center">
                                            <div class="col-md-2 col-lg-2 col-xl-2">
                                                <img src="@item.Image"
                                             class="img-fluid rounded-3" alt="Cotton T-shirt">
                                            </div>
                                            <div class="col-md-3 col-lg-3 col-xl-3">
                                                <p class="lead fw-normal mb-2">@item.Name</p>
                                                <p><span class="text-muted">@item.Summary</span></p>
                                            </div>
                                            <div class="col-md-3 col-lg-2 col-xl-2 offset-lg-1">
                                                <h5 class="mb-0">@item.Price лв.</h5>
                                            </div>
                                            <div class="col-md-1 col-lg-1 col-xl-1 text-end">
                                                <a asp-action="RemoveFromCart" asp-controller="Customer" asp-route-id="@item.Id"><i class="fas fa-trash fa-lg"></i></a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                            <div class="card">
                                <div class="card-body">
                                    <a class="btn btn-success" asp-action="Order" asp-controller="Customer">Поръчай!</a>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            </section>
            <div class="section-header">
                <p>НАШЕТО <span>МЕНЮ</span></p>
            </div>

            <ul class="nav nav-tabs d-flex justify-content-center" data-aos="fade-up" data-aos-delay="200">

                <li class="nav-item">
                    <a class="nav-link active show" data-bs-toggle="tab" data-bs-target="#menu-salata">
                        <h4>Салата</h4>
                    </a>
                </li><!-- End tab nav item -->

                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-predqstie">
                        <h4>Предястие</h4>
                    </a><!-- End tab nav item -->

                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-osnovno">
                        <h4>Основно ястие</h4>
                    </a>
                </li><!-- End tab nav item -->

                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-bbq">
                        <h4>Барбекю</h4>
                    </a>
                </li><!-- End tab nav item -->
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-pizza">
                        <h4>Пица</h4>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-rizoto">
                        <h4>Ризото</h4>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-pasta">
                        <h4>Паста</h4>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" data-bs-target="#menu-dessert">
                        <h4>Десерт</h4>
                    </a>
                </li>
            </ul>

            <div class="tab-content" data-aos="fade-up" data-aos-delay="300">

                <div class="tab-pane fade active show" id="menu-salata">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Салата</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Салата")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>
                                </div>
                            }
                        }

                    </div>
                </div>

                <div class="tab-pane fade" id="menu-predqstie">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Предястие</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Предястие")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div>

                <div class="tab-pane fade" id="menu-osnovno">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Основно ястие</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Основно ястие")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div>
                <div class="tab-pane fade" id="menu-bbq">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Барбекю</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Барбекю")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div>
                <div class="tab-pane fade" id="menu-pizza">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Пица</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Пица")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div>

                <div class="tab-pane fade" id="menu-rizoto">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Ризото</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Ризото")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div>

                <div class="tab-pane fade" id="menu-pasta">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Паста</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Паста")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div>

                <div class="tab-pane fade" id="menu-dessert">

                    <div class="tab-header text-center">
                        <p>Menu</p>
                        <h3>Десерт</h3>
                    </div>

                    <div class="row gy-5">

                        @foreach (var item in Model)
                        {
                            if (item.Category.Name == "Десерт")
                            {

                                <div class="col-lg-4 menu-item">
                                    <a href="/Menu/Details/@item.Id"><img src="@Html.DisplayFor(modelItem => item.Image)" class="menu-img img-fluid" alt=""></a>
                                    <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                                    <p class="ingredients">
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                    <p class="price">
                                        @Html.DisplayFor(modelItem => item.Price) лв.
                                    </p>
                                    <p class="price">
                                        Категория: @Html.DisplayFor(modelItem => item.Category.Name)
                                    </p>
                                    <a class="btn btn-primary" asp-action="AddToCart" asp-controller="Customer" asp-route-id="@item.Id">Добави в количката</a>

                                </div>
                            }
                        }
                    </div>
                </div><!-- End Breakfast Menu Content -->
                <!-- End Starter Menu Content -->

            </div>

        }
        else
        {

            <div class="section-header">
                <p>Изберете маса, след което изберете от <span>нашето МЕНЮ</span></p>
            </div>
        }
    </div>

</section>